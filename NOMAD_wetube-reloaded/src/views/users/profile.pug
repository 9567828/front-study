extends ../base
include ../mixins/video

block content
  div.profile-data
    img(src=`/${user.avatarUrl}`).avatar
    h3=user.username
  div.video-grid
    each video in user.videos
      +video(video)
    else
      span.empty-message 영상이 없습니다.